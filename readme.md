# To run

'''
yarn or npm install
yarn dev if you have nodemon as a global module, you can install nodemon by using the command npm install -g nodemon
or yarn start
'''

you can change port to run this website as you like, you can also create a .env file to set your environment variables, by default i have just set port = 5000.
